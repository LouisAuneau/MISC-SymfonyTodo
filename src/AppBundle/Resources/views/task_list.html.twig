{% extends '::base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="{{ asset('css/task_list.css') }}">
{% endblock %}

{% block body %}
    <div id="container">
        <h2>Vos Tâches :</h2>
        <hr/>
        <ul>
        {% for task in tasks %}
            <li><a href="{{ path('todo_see_task', {'id': task.id}) }}">{{ task.title }}</a></li>
        {% endfor %}
        </ul>
    </div>
{% endblock %}